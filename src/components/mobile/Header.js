import React from 'react';
import styles from './Header.css';

const Header = () => (
    <nav
        className={styles.header}
    >
        this mobile is header!!
    </nav>
);

export default Header;
